dataset:
  datamodule: MNISTDataModule
  batch_size: 64
  data_dir: ../../../data
optimizer:
  type: AdamW
  lr: 0.001
outputs:
  output_dir: outputs
trainer:
  max_epochs: 10
  refresh_rate: 50
model:
  model_type: SimpleMLP
  hidden_size: 20
experiment_name: mnist_example
